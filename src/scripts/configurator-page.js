import { gsap, ScrollTrigger } from "./gsap-setup.js";

// Configurator page animations — DRAFT.
// Client direction isn't finalized yet; nothing to animate until the
// design handoff lands. Import stays wired so the page's <script> tag
// has somewhere to point.
